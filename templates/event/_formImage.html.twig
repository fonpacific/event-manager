

<div class="row">
    <div class="col">
        {{ form_start(form) }}
            {{ form_row(form.immagine) }}

            <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form, {'render_rest' : true}) }}
    </div>
</div>